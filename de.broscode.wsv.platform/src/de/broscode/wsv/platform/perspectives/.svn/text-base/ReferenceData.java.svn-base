package de.broscode.wsv.platform.perspectives;

import org.eclipse.ui.IPageLayout;
import org.eclipse.ui.IPerspectiveFactory;

import de.broscode.wsv.platform.ids.PlatformIDs;

public class ReferenceData implements IPerspectiveFactory {
	
	public void createInitialLayout(IPageLayout layout) {
		layout.addView(PlatformIDs.VIEW_ID_RESIDENT_LIST,
		IPageLayout.LEFT, 0.275f, layout.getEditorArea());
	}
}
